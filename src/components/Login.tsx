import React from 'react';
import { Text, View } from 'react-native';

const LoginScreen: React.FC = () => (
  <View className="bg-purple-700">
    <Text className='text-yellow-50'>Login</Text>
  </View>
)

export default LoginScreen;